# selenium_webdriver — Chrome Scraper Architecture Blueprint

## Project Overview

- **Project Name:** selenium_webdriver
- **Project Type:** Web Scraper / Browser Automation
- **Architecture Pattern:** Node.js Script (Selenium)
- **Technology Stack:** Node.js 18+ + Selenium WebDriver
- **Package Manager:** npm
- **Stack Type:** Node.js

## Technology Stack Details

| Category | Technology |
|---|---|
| Runtime | Node.js 18+ |
| Language | JavaScript (ES Modules) |
| Browser Automation | Selenium WebDriver 4.x |
| Browser | ChromeDriver |
| Formatting | Prettier (2-space indent) |
| Testing | Manual (node script execution) |
| CI/CD | GitHub Actions |

## Directory Structure

```
selenium_webdriver/
├── AGENTS.md                 # Project context
├── package.json              # Node dependencies
├── bun.lock                  # Bun lockfile
├── src/
│   └── scrape.js             # Main scraper script
├── node_modules/             # Dependencies
├── .github/workflows/        # CI pipeline
│   └── ci.yml
└── .vscode/                  # VS Code config
```

## Architectural Patterns

### Single-Script Scraper Pattern
- Single entry point (`src/scrape.js`) for all scraping operations
- ES Modules (`import`/`export`) for modern JavaScript
- Direct execution via `node src/scrape.js`

### Reliable Automation Patterns
1. **Explicit WebDriverWait** — Never use `sleep()`; always use `WebDriverWait` with expected conditions
2. **Retry on StaleElement** — Catch `StaleElementReferenceException` and retry
3. **Consistent selectors** — Use `By` selectors consistently for element location
4. **Cleanup in finally** — Always call `driver.quit()` in `finally` block for resource cleanup

### Polite Scraping
- Respect `robots.txt`
- Polite delays between requests
- User-agent rotation support

## Data Flow

```
npm test / node src/scrape.js
              ↓
    Selenium WebDriver Instance
              ↓
        ChromeDriver (Chrome)
              ↓
    ┌─────────┴─────────┐
    ↓                   ↓
Navigate URLs     Extract Data
    ↓                   ↓
WebDriverWait     DOM Queries
    ↓                   ↓
Page Loaded       Data Collected
    ↓                   ↓
    └─────────┬─────────┘
              ↓
    Console Output / File
              ↓
      driver.quit() (finally)
```

## Key Dependencies

- **Runtime:** `selenium-webdriver@4.x`
- **Browser:** ChromeDriver (Chrome)
- **Dev:** `prettier`

## Architecture Decisions

1. **Selenium over Scrapy** — For JavaScript-rendered pages requiring browser automation
2. **ES Modules** — Modern JavaScript module system (type: "module" in package.json)
3. **No build step** — Direct Node.js execution without transpilation
4. **No deployment** — Local execution only; no server deployment
5. **Manual testing** — Visual verification via console output

## Extensibility Points

- Add additional scraping functions for different target sites
- Implement data persistence (file storage, database)
- Add proxy rotation and user-agent rotation
- Convert to CLI tool with argument parsing
- Add headless mode support

---

*Generated by architecture-blueprint-generator — comprehensive analysis*
