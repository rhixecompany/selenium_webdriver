# Workflow Analysis — Selenium WebDriver (Chrome Scraper)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Selenium WebDriver
**Type:** Browser Automation / Web Scraper
**Stack:** Node.js 18+ + Selenium WebDriver 4.x + ChromeDriver
**Architecture Pattern:** Script-based Selenium Automation
**Entry Point:** `node src/scrape.js`

A Node.js Selenium-based web scraper for comics/manga targeting JavaScript-rendered pages, with explicit wait patterns and retry logic.

## Workflows

### Workflow 1: Manga/Comic Page Scraping

**Description:** Automated scraping of JavaScript-rendered manga/comic pages using Selenium WebDriver.

**Entry Points:**
- `node src/scrape.js` — main scraping script
- `node src/scrape2.js` — alternative scraper variant

**Steps:**
1. User runs `node src/scrape.js` or `npm test` (runs scrape.js)
2. Script initializes ChromeDriver via Selenium WebDriver
3. Navigates to target manga/comic URL
4. Uses `WebDriverWait` for dynamic content to fully render
5. Extracts comic metadata (title, chapter, pages)
6. Downloads/processes image data from rendered elements
7. Handles `StaleElementReferenceException` with retry pattern
8. Saves scraped data to local filesystem
9. Ensures `driver.quit()` in `finally` block

**Sequence Diagram:**
```
User                    scrape.js                Selenium/ChromeDriver         Target Website
  |                        |                            |                          |
  |-- node scrape.js ----->|                            |                          |
  |                        |                            |                          |
  |                        |-- new Builder() --------->|                          |
  |                        |   .forBrowser('chrome')    |                          |
  |                        |   .build()                 |                          |
  |                        |<-- WebDriver instance -----|                          |
  |                        |                            |                          |
  |                        |-- driver.get(url) -------->|                          |
  |                        |                            |-- HTTP request ---------->|
  |                        |                            |<-- HTML + JS --------------|
  |                        |                            |                          |
  |                        |-- WebDriverWait ---------->|                          |
  |                        |   for element to render    |                          |
  |                        |                            |                          |
  |                        |-- extract data ----------->|                          |
  |                        |   (findElement,            |                          |
  |                        |    getAttribute,           |                          |
  |                        |    getText)                |                          |
  |                        |                            |                          |
  |                        |<-- scraped data ----------|                          |
  |                        |                            |                          |
  |                        |-- process + save --------->|                          |
  |                        |   data to filesystem       |                          |
  |                        |                            |                          |
  |                        |-- driver.quit() ---------->|                          |
  |                        |   (finally block)          |                          |
  |                        |                            |                          |
  |<-- scrape complete ----|                            |                          |
```

**Error Handling:**
- `StaleElementReferenceException` → retry element lookup up to 3 times
- Element not found → continue or skip, log warning
- ChromeDriver not installed → clear error message
- `driver.quit()` always called in `finally` block (never leaked)
- `robots.txt` compliance + polite delays between requests

**Test Patterns:**
- Manual testing via `node src/scrape.js`
- `npm test` configured to run scrape.js as test
- Consistent `By` selectors for maintainability

---

### Workflow 2: Multi-Target Batch Scrape

**Description:** Batch scraping of multiple pages or targets using utility helpers.

**Entry Points:**
- `src/utils.js` — shared utility functions
- `src/test1.js` — additional scraper variant

**Steps:**
1. Script reads list of target URLs from config
2. Initializes ChromeDriver with profiles/options
3. Iterates through targets with polite delays between each
4. For each target: wait, extract, save
5. Aggregates results and creates summary report
6. Cleans up driver resources

**Error Handling:**
- Per-target failure → log error, continue with next target
- Rate limiting detection → adaptive delay increase
- Network errors → retry with exponential backoff
